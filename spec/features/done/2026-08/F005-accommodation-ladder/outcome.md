# Outcome

## Result

On `TEXT_OVERFLOW` / `CONTENT_OVERFLOW` / `OUTSIDE_SAFE_AREA`, prove climbs landscape → square → portrait and records `steppedFrom`. Every attempt is a full re-validate. Refusals speak in characters, lines, and information units as well as pixels. `NEAR_OVERFLOW` and `TITLE_STEPPED` are non-blocking warnings.

## Date

2026-08-19

## References

- `src/evaluate.js`, `src/text.js` (`fit`), `src/validate.js`, `src/solve.js`
- `src/evaluate.test.ts`
