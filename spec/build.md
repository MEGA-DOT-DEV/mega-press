# Build

> Coordinator-maintained narrative board. Reconcile it with the planned and active feature folders before selecting, starting, resuming, reviewing, merging, proving, or dropping work. Update it in the same coherent change that changes feature state. Drift is an advisory, never a runtime gate. Keep entries short, concrete, and decision-useful.

Status marks are prose only: 🟠 ACTIVE · 🔴 PLANNED · 🟢 PROVEN · ⚪ DROPPED. Nothing in `limen` treats them as workflow state.

## TRACK

- Check→render is now the promised loop: exit `0` means the plate locked geometrically, exit `1` is a named refusal, accommodations are enumerable states `validate()` re-checks. Next stretch is whatever the host still cannot do with that loop.

## NOW

- (nothing active)

## NEXT

- (nothing queued)

## PROVEN

- `F001-render-refusal-reporting` — `press render` geometry failures exit `1` with named codes and write no file. → `spec/features/done/2026-08/F001-render-refusal-reporting/`
- `F002-compile-contract-parity` — compile honors advertised min/max; over-max is a named refusal, never a silent `.slice()`. → `spec/features/done/2026-08/F002-compile-contract-parity/`
- `F003-deterministic-text-metrics` — baked glyph-advance table; `measure()` is identical in Node and browser. → `spec/features/done/2026-08/F003-deterministic-text-metrics/`
- `F004-geometric-press-check` — `press check` runs solve + validate headless. → `spec/features/done/2026-08/F004-geometric-press-check/`
- `F005-accommodation-ladder` — landscape→square→portrait, author-currency refusals, `NEAR_OVERFLOW` / `TITLE_STEPPED` warnings. → `spec/features/done/2026-08/F005-accommodation-ladder/`
- `F006-sanity-caps` — generous `maxLength` garbage guards with named codes, sized against portrait. → `spec/features/done/2026-08/F006-sanity-caps/`
- `F007-responsive-mount` — `ResizeObserver` + `aspect-ratio` + 340px legibility floor. → `spec/features/done/2026-08/F007-responsive-mount/`
