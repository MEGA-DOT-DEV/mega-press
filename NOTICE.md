# @mega/press

Portable MEGA plate engine + template kernel.

Engine source (src/*.js, components, vendor) was vendored from mega-content/lab press, then owned in mega-experience `packages/press`, then moved here as the single package.

Do not “improve spacing” in React CSS twins — change the press or the spec.
Upstream design system: mega-content/lab. Re-sync deliberately when lab press changes.
